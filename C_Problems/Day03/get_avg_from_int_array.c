#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

double solution (int numbers[], size_t numbers_len){
    double amswer = 0;
    int i = 0;

    while (i != numbers_len){
        answer += numbers[i];
        i++;
    }
    answer /= (double)numbers_len;

    return answer;
}
